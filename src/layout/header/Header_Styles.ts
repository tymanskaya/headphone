import styled, {css} from "styled-components";


const Header = styled.header`
    padding: 20px 0;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 999999;
    background: inherit;
`

export const S = {
    Header
}